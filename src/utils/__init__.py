"""Utility package for config, registries, logging, checkpoints, paths, and sanity.

Use specific utility modules directly so dependencies stay clear and startup side
effects stay minimal.

Typical usage:
    from src.utils.registry import MODEL_REGISTRY
    from src.utils.sanity import run_sanity_checks
"""
