"""Python package marker for console-script access to repository scripts.

Most users should run shell scripts directly or use ``uv run python`` commands,
but this package marker allows pyproject console entry points to import script
modules such as ``scripts.run_sanity``.
"""
